-- Check status yang perlu di-validate
-- Placeholder query - replace dengan query sebenarnya
SELECT 
    'status_001' AS status_code,
    'PENDING' AS status_name,
    'Need validation' AS description,
    CURRENT_TIMESTAMP AS created_at
